import { Component } from '@angular/core';

@Component({
  // No need for a selector because it will be loaded via routing
  templateUrl: './login.component.html'
})
export class LoginComponent {
  // isLoading = false;

}
